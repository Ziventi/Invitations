<main class="notices">
  <div class="main">
    <section>
      <ol class="notices">
        <% resources.notices.forEach((rule) => { %>
        <li>
          <strong><%= rule.summary %>:</strong>
          <span><%= rule.description %></span>
        </li>
        <% }); %>
      </ol>
    </section>
  </div>

  <%- include('/templates/partials/footer.ejs') %>
</main>
